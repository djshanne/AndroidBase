<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
    </style>

    <style name="GenericTitles" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/white</item>
    </style>


    <style name="GenericTextDark" parent="GenericText">
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="GenericText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="HorizontalSections">
        <item name="android:background">@drawable/background_gradient_vertical</item>
    </style>

    <style name="GenericListTheme" parent="AppTheme">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
    </style>

    <style name="AppTheme.Gradient" parent="Widget.AppCompat.Toolbar">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:background">@drawable/background_gradient_horizontal</item>
    </style>

    <style name="ToolbarTitle" parent="GenericTitles">
        <item name="android:gravity">center_vertical</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lines">1</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">@dimen/base_text_size_big</item>
    </style>

    <style name="TabLayoutStyle" parent="Widget.Design.TabLayout">
        <!--<item name="tabMaxWidth">@dimen/tab_max_width</item>-->
        <item name="tabIndicatorColor">@android:color/white</item>
        <!--<item name="tabIndicatorHeight">2dp</item>-->
        <!--<item name="tabPaddingStart">8dp</item>-->
        <!--<item name="tabPaddingEnd">8dp</item>-->
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <!--<item name="android:textSize">14sp</item>-->
        <item name="android:textColor">@color/gray2</item>
        <item name="textAllCaps">true</item>
    </style>





</resources>
